# hub

Todavía no tiene código propio. A partir de la Fase 5, este paquete va a
contener la configuración de despliegue (Docker Compose, variables de
entorno) para correr `apps/api` con `ROLE=hub` en la mini-PC del local.

No duplica lógica: reutiliza el mismo binario de `apps/api` (ver la regla
de arquitectura en `CLAUDE.md`).
